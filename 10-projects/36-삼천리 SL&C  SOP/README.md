# 삼천리 SL&C 브랜드별 SOP — 직원 교육 앱

매장 교육자료(PPTX)를 **목차 순서 그대로** 옮긴 웹앱. 직원이 **개인 스마트폰**으로 학습하고 이수를 표시한다.
1차 대상은 **KSC(Kalbi Social Club)** 와 **WASA(Robata Wasa)**.

배포: https://comimi12.github.io/slnc-sop-dashboard/

## 무엇이 들어있나

| 브랜드 | 문서 | 분량 |
|--------|------|------|
| KSC | 입문 · 서비스 매뉴얼 | 49p / 8개 챕터 |
| KSC | 입문 매뉴얼 (간소화) | 18p |
| KSC | 메뉴 SOP | 54종 (사진 37종) |
| WASA | 입문 · 서비스 매뉴얼 | 52p / 8개 챕터 |
| WASA | 메뉴 SOP | 90종 (사진 88종) |

원본 이미지 184장을 웹용(긴 변 1000px)으로 재인코딩해 포함.

## 화면 (왼쪽 레일 6탭)

| 탭 | 내용 |
|----|------|
| **홈** | 내 진도율, 이어서 학습할 항목 |
| **SOP** | 입문 매뉴얼 (간소화) — 번호순 15~16개 항목 |
| **메뉴** | 사진 카드 → 구성·설명·추천 스크립트·알레르기 |
| **체크** | 오픈/마감 체크리스트 (날짜 바뀌면 자동 초기화) |
| **수료** | 내 정보(이름·연락처·포지션) · 남은 항목 · 트레이너 서명 · 확인코드 |
| **이수확인** | 팀 수료 현황 (관리자용, 항상 노출) |

- 헤더에서 **한 / 한+EN / EN** 보기 전환 (기본 한국어)
- 검색은 헤더 돋보기 — 간소화 과정에 있는 내용만 검색된다
- 다크 모드 자동 + 수동 토글 (레일 하단)
- 홈 화면에 추가하면 앱처럼 실행 (PWA), 오프라인 동작

## 입문 매뉴얼 (간소화)

휴대폰으로 훑는 교육이라 전체 매뉴얼(49~52p)이 아니라 꼭 필요한 장만 순서대로 남겼다.
구성은 `src/app.js`의 `COURSE` 배열 한 곳에서 정한다 — 항목을 더하거나 빼려면 여기만 고치면 된다.

| 묶음 | 항목 |
|------|------|
| 브랜드 · 매장 | 브랜드 소개, 우리 매장 정보 |
| 기본 기준 | 용모 · 복장 기준, 직급별 역할 |
| 내 포지션 | 선택한 직급의 표준 절차 1장 |
| 서비스 | 서비스 10단계, 상황별 응대, 컴플레인 응대 |
| 안전 · 법규 | 주류, 알러지, 위생, 비상 대응, 근로 법규 |
| 체크리스트 | 오픈, 마감, 메뉴별 알러지 표(KSC) |

**뺀 것**: 표지·목차, 매뉴얼 사용법, 서비스 철학, 보고 체계, 1일 운영 리듬,
관리자 SOP(CH.05) 전체, 응대 스크립트 ①②, 서비스 10원칙, 교육 로드맵,
WASA의 미완성 템플릿 페이지(MENU KNOWLEDGE ①②).

본문도 간소화했다 — 기본 한국어만 보이고, 제목 없는 긴 도입 문단은 뺀다.
영문이 필요하면 헤더에서 `한+EN` 또는 `EN`으로 바꾼다.

## 교육 이수 시스템

서버도 로그인도 없다. 기록은 **그 휴대폰 안**(localStorage)에만 남는다.

1. **내 정보 등록** — 이름 + 연락처 뒤 4자리 + 포지션 (동명이인 구분용)
2. **필수 코스 자동 구성** — 간소화 과정 + 메뉴 카테고리 전부
3. **학습 완료 체크** — 페이지 하단 버튼, 진도율 실시간 반영
4. **트레이너 서명** — 전 항목 완료 시에만 활성화. 손가락 서명 + 담당자 이름
5. **확인코드 6자리** — 이름·포지션·완료수·서명일로 생성. 매니저가 대조
6. **공유** — 수료 내용을 카톡 등으로 전송 (`navigator.share`, 미지원 시 클립보드)

서명 후 학습 항목을 바꾸면 서명이 **자동 무효 처리**되어 다시 받아야 한다.

### 이수확인 화면 (`#/admin`)

직원이 카톡으로 보낸 수료 보고를 **그대로 붙여넣으면** 팀 현황 표가 만들어진다. 여러 건을 한꺼번에 붙여넣어도 건별로 잘라 읽는다.

- 확인코드를 **다시 계산해 대조** → 이름·진도·서명일을 고치면 `코드 불일치`로 잡힌다 (코드의 씨앗이 보고서 본문에 전부 들어 있기 때문)
- 수료로 세는 것은 **코드가 검증된 보고만**
- `현황 표 복사` → 엑셀에 바로 붙여넣는 TSV
- 같은 사람(브랜드+이름+포지션)의 보고가 다시 들어오면 갱신

이 현황도 **그 기기에만** 저장된다. 여러 매니저가 실시간으로 공유하려면 공용 저장소가 필요하다(아래 확장 참고).

## 구조

```
36-삼천리 SL&C  SOP/
├── src/                    # 배포되는 사이트 그대로
│   ├── index.html
│   ├── app.js              # 라우팅 · 렌더링 · 진도/서명 저장 · 검색
│   ├── style.css           # 따뜻한 교육자료 톤 + 다크 모드
│   ├── data.json           # 추출 결과 (약 550KB)
│   ├── img/                # 사진 184장
│   ├── manifest.webmanifest, sw.js, icon.png   # 홈 화면 추가 · 오프라인
├── build/
│   ├── extract.py          # PPTX → data.json + img/  (원본 수정 시 재실행)
│   ├── verify.py           # 14개 화면 자동 캡처 + 콘솔오류·가로스크롤·깨진이미지 검사
│   ├── deploy.py           # src/ → GitHub Pages 저장소 push
│   └── shots/              # verify.py 캡처 결과
└── _deploy/                # 배포용 클론 (git 무시)
```

## 원본 교육자료

`C:\Users\owner\Desktop\교육팀\4. 신규매장매뉴얼, 교안\오픈매장 매뉴얼\` 아래

- `KSC_US\교육 자료\KSC_BREA_입문매뉴얼.pptx`
- `KSC_US\교육 자료\KSC_BREA_입문매뉴얼_간소화.pptx`
- `KSC_US\교육 자료\KSC_menu_sop_메뉴교육자료.pptx`
- `WASA\WASA_BREA_Training_Manual_A4.pptx`
- `WASA\WASA_menu_manual_전체90종.pptx`

## 갱신 방법

```bash
python build/extract.py                        # 원본 PPTX 다시 추출
python -m http.server 8766 --directory src     # 로컬 확인
python build/verify.py 8766                    # 화면 자동 검증
python build/deploy.py                         # 배포
python build/verify.py https://comimi12.github.io/slnc-sop-dashboard/   # 배포본 검증
```

`extract.py`는 원본 덱이 생성기로 만들어져 도형 배치가 일정하다는 점을 이용한다.
레이아웃을 크게 바꾼 PPTX를 넣으면 추출 규칙(카드 밴드 폭, `CL_*` 도형 이름, 챕터 푸터 `CH.0n`)을 함께 손봐야 한다.

브랜드 추가는 `build/extract.py` 상단 `DECKS`에 항목 추가 (`kind`는 `manual` 또는 `menu`).

## 앞으로 가능한 확장

- **공용 저장소** — 지금 관리자 화면은 기기 로컬이다. Apps Script Web App(또는 작은 API)에 `certText()`를 POST하고 관리자 화면이 GET으로 읽으면, 붙여넣기 없이 여러 매니저가 같은 현황을 본다
- **확인 퀴즈** — 챕터별 3문항, 통과해야 완료 처리
- **QR 출석** — 매장 QR을 찍어야 그날 체크리스트가 열리도록
- **트레이너 화면** — 여러 직원의 확인코드를 한 번에 대조

## 주의

본 자료는 **SIM US의 자산**이며 원본에 무단 복제·배포·공개·외부 반출 금지가 명시되어 있다.
현재 공개(Public) GitHub Pages로 배포되어 있으므로, 링크를 아는 사람은 누구나 열람할 수 있다.
